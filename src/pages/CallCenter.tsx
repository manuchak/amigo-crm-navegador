
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const CallCenter = () => {
  return (
    <div className="container mx-auto p-4 py-20">
      <Card>
        <CardHeader>
          <CardTitle>Centro de Llamadas</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Página del centro de llamadas en construcción.</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default CallCenter;
